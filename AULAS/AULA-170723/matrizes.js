const matriz3x3 = [
    [1, 8, 4],
    [6, 5, 9],
    [2, 3, 7]
];

for(let x = 0; x < matriz3x3.length; x++ ){
    for(let y = 0; y < matriz3x3[x].length; y++){
        console.log(matriz3x3[x][y])
    }
}